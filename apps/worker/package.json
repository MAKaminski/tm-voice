{
  "name": "@tm/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/main.ts",
    "start": "tsx src/main.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "replay": "tsx src/replay.ts"
  },
  "dependencies": {
    "@tm/adapters": "workspace:*",
    "@tm/api": "workspace:*",
    "@tm/compliance": "workspace:*",
    "@tm/db": "workspace:*",
    "@tm/shared": "workspace:*",
    "bullmq": "^5.34.0",
    "drizzle-orm": "^0.44.0",
    "ioredis": "^5.4.0",
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
